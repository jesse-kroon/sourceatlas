[package]
name = "repolens"
version = "0.1.0"
edition = "2024"
description = "A small CLI app that generates a report on directories"
license = "MIT"

[dependencies]
tempfile = "3.27.0"

[dev-dependencies]
tempfile = "3.27.0"
